const buttons=document.querySelectorAll('[data-view]');const views=document.querySelectorAll('.view');function show(id){views.forEach(v=>v.classList.toggle('active',v.id===id));buttons.forEach(b=>b.classList.toggle('active',b.dataset.view===id));window.scrollTo({top:0,behavior:'smooth'})}buttons.forEach(b=>b.addEventListener('click',()=>show(b.dataset.view)));document.querySelectorAll('[data-go]').forEach(b=>b.addEventListener('click',()=>show(b.dataset.go)));const modal=document.getElementById('modal');document.getElementById('newAppointment').onclick=()=>modal.classList.add('open');document.querySelector('.close').onclick=()=>modal.classList.remove('open');modal.addEventListener('click',e=>{if(e.target===modal)modal.classList.remove('open')});async function clients(){const list=document.getElementById('clientsList');try{const r=await fetch('../backend/api/clients.php');if(!r.ok)throw 0;const data=await r.json();const rows=Array.isArray(data)?data:(data.clients||data.data||[]);document.getElementById('clientCount').textContent=rows.length;if(!rows.length){list.innerHTML='<p class="loading">La base está lista. El primer cliente aparecerá aquí.</p>';return}list.innerHTML=rows.slice(0,20).map((c,i)=>{const name=c.name||c.full_name||c.nombre||`Cliente ${i+1}`;const phone=c.phone||c.telefono||c.whatsapp||'Sin teléfono';const initials=name.split(' ').slice(0,2).map(x=>x[0]).join('').toUpperCase();return `<div class="client"><i>${initials}</i><div><b>${name}</b><span>${phone}</span></div><small>Ver ficha →</small></div>`}).join('')}catch(e){document.getElementById('clientCount').textContent='0';list.innerHTML='<p class="loading">Base conectada. Aún no hay clientes para mostrar.</p>'}}clients();