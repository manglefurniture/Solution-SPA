(()=>{
const $=(s,r=document)=>r.querySelector(s);const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
function toast(message,type='ok'){let box=$('#uxToast');if(!box){box=document.createElement('div');box.id='uxToast';box.className='ux-toast';document.body.appendChild(box)}box.textContent=message;box.className=`ux-toast show ${type}`;clearTimeout(box._t);box._t=setTimeout(()=>box.classList.remove('show'),2600)}
window.uxToast=toast;
function enhanceEmptyStates(){document.querySelectorAll('.empty-note').forEach(el=>{if(el.dataset.ux)return;el.dataset.ux='1';if(/no hay citas|aún no hay citas/i.test(el.textContent)){el.innerHTML=`<span class="empty-icon">◷</span><b>No hay citas aquí</b><small>Usa <strong>+ Nueva cita</strong> cuando quieras agendar una.</small>`}else if(/no hay clientes|aún no hay clientes/i.test(el.textContent)){el.innerHTML=`<span class="empty-icon">♡</span><b>Aún no hay clientes</b><small>Usa <strong>+ Nuevo cliente</strong> para registrar el primero.</small>`}else if(/no hay servicios|aún no hay servicios/i.test(el.textContent)){el.innerHTML=`<span class="empty-icon">✦</span><b>Aún no hay servicios</b><small>Usa <strong>+ Nuevo servicio</strong> para crear el primero.</small>`}})}
const observer=new MutationObserver(()=>enhanceEmptyStates());observer.observe(document.body,{subtree:true,childList:true});enhanceEmptyStates();
const modal=$('#modal');if(modal){const close=()=>modal.classList.remove('open');document.addEventListener('keydown',e=>{if(e.key==='Escape'&&modal.classList.contains('open'))close()});}
const form=$('#contextForm');if(form){form.addEventListener('invalid',e=>{e.preventDefault();const field=e.target;field.classList.add('field-error');field.focus();const label=field.closest('label')?.childNodes?.[0]?.textContent?.trim()||'este campo';toast(`Revisa ${label.toLowerCase()} para continuar`,'error')},true);form.addEventListener('input',e=>e.target.classList.remove('field-error'))}
const add=$('#contextAdd');if(add){add.addEventListener('click',()=>setTimeout(()=>{const first=$('#modal input:not([disabled]),#modal select:not([disabled]),#modal textarea');first?.focus()},120))}
document.addEventListener('click',e=>{const cancel=e.target.closest('[data-status="cancelled"],.cancel-appointment');if(cancel&&!confirm('¿Cancelar esta cita? La cita quedará registrada como cancelada.')){e.preventDefault();e.stopImmediatePropagation()}},true);
})();